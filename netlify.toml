# Build settings
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Environment variables (configure in Netlify dashboard)
# VITE_CLERK_PUBLISHABLE_KEY
# VITE_API_URL

# Redirects for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
